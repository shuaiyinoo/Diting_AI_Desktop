# Diting AI Desktop v5.1.4 更新

> 本次更新为 CI 流水线优化版本，通过缓存策略、依赖安装优化、前端构建复用、资源过滤等多项措施显著缩短发版时间。

---

## 🚀 CI 流水线优化

### 优化1：npm + Electron 二进制缓存
- 所有平台 Job 启用 `actions/setup-node` 内置 npm 缓存
- Electron 二进制（~270MB）通过 `actions/cache` 缓存，避免每次重新下载

### 优化2：npm ci 替代 npm install
- 从 `.gitignore` 移除 `package-lock.json`，提交 lock 文件
- `npm ci` 比 `npm install` 快 2-3 倍（跳过依赖树解析，确定性安装）

### 优化3：前端构建提取为独立 Job
- 前端产物（HTML/JS/CSS）和 Electron 主进程 bundle 是平台无关的
- 构建一次后通过 artifact 供 4 个平台 Job 下载复用
- macOS ARM64 和 x64 不再各自重复构建前端

### 优化4：按平台排除非目标资源
- 4 个 builder 配置添加文件过滤规则
- 排除 README、LICENSE、CHANGELOG、test/tests/docs 等无关文件
- 减少打包体积和压缩时间

### 优化5：merge-mac-yml 通过 artifact 传递 SHA
- macOS 两个架构的 zip 通过 GitHub Actions artifact 传递给 merge Job
- 不再从 GitHub Release 下载 300MB+ zip 仅为计算 SHA512

### 优化6：分离打包和上传
- electron-builder 使用 `--publish never` 仅打包（含签名+公证）
- 上传到 Release 分离为独立步骤
- 上传失败重试 3 次时不重复打包

---

## 📥 下载

- **macOS Apple Silicon** — `Diting-mac-5.1.4-arm64.dmg`
- **macOS Intel** — `Diting-mac-5.1.4.dmg`
- **Windows** — `Diting-Setup-5.1.4.exe`
- **Linux** — `Diting-5.1.4.deb`
