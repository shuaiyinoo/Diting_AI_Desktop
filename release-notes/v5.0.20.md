# Diting AI Desktop v5.0.20 更新

## 修复

- **GitHub Actions 管线修复** — 修复 `npm ci` 因缺少 `package-lock.json` 而失败的问题，改为 `npm install`
- **esbuild 构建修复** — 修复 `.gitignore` 中 `runtime/` 规则误伤 `electron/components/pi/runtime/` 源码目录的问题
- **macOS x64 管线优化** — 将 x64 构建从 `macos-13` 改为 `macos-latest` 交叉编译，4 个平台并行执行
- **latest-mac.yml 合并修复** — 参考 Proma 方案修复 merge Job：`electron-builder --publish always` 创建的是 draft release，无法通过 `/releases/tags/{tag}` 查找，改回 `/releases` 列表遍历；保留 asset 调试输出和文件名 URL 解码

## 下载

- **macOS Apple Silicon** — `Diting AI-mac-5.0.20-arm64.dmg`
- **macOS Intel** — `Diting AI-mac-5.0.20-x64.dmg`
- **Windows** — `Diting AI-Setup-5.0.20.exe`
- **Linux** — `Diting AI-5.0.20.AppImage`
