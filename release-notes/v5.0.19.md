# Diting AI Desktop v5.0.19 更新

## 修复

- **GitHub Actions 管线修复** — 修复 `npm ci` 因缺少 `package-lock.json` 而失败的问题，改为 `npm install`
- **esbuild 构建修复** — 修复 `.gitignore` 中 `runtime/` 规则误伤 `electron/components/pi/runtime/` 源码目录的问题
- **macOS x64 管线优化** — 将 x64 构建从 `macos-13` 改为 `macos-latest` 交叉编译，4 个平台并行执行
- **latest-mac.yml 合并修复** — 重写 merge Job：改用 `/releases/tags/{tag}` API 兼容 draft release；添加 asset 名称调试输出；Python 匹配时做 URL 解码处理文件名空格；支持删除多个旧 yml

## 下载

- **macOS Apple Silicon** — `Diting AI-mac-5.0.19-arm64.dmg`
- **macOS Intel** — `Diting AI-mac-5.0.19-x64.dmg`
- **Windows** — `Diting AI-Setup-5.0.19.exe`
- **Linux** — `Diting AI-5.0.19.AppImage`
